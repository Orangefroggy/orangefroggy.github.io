# orangefroggy.github.io
This takes any photo you upload and gradually reshuffles it into a target image, using only the colors already there. A small evolutionary algorithm keeps random pixel swaps that make it look a little more like the target, so you watch it morph in real time, right in the browser, up to 4096x4096.
